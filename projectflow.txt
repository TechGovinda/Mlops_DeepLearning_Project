1. create template file and run it ---- to create a template of the code
2. conda create --name myenv python=3.10.16
3. pip install -r requirements.txt  -- on powershell/command prompt
4. add code on utils/common.py   -- write a functions which commonly used 

5. Data Ingestion
  -update config.yaml
  -update params.yaml
  -update the entity
  -update the Configuration manager in src config
  -update the components
  -update the pipeline
  -update the main.py
  -update the app.py

  